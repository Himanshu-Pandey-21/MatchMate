# Data

MatchMate generates `teams.csv`, `players.csv`, and `matches.csv` in the working directory when it runs.

These runtime data files are intentionally excluded from version control through `.gitignore`.

If you want to demonstrate the project publicly, use fictional/sample data rather than personal student information.
